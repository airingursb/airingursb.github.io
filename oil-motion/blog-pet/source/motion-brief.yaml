concept_contract: source/concept-contract.yaml
identity_bible: source/identity-bible.md
parameter_space: circular
media_access: random
gesture_policy:
  unit: continuous
  inertia: coalesce
  while_active: retarget
  boundary: loop
  programmatic_navigation: ignore
storyboard: "screen-top-left -> top -> top-right -> right -> bottom-right -> bottom -> bottom-left -> left -> screen-top-left"
keyframes:
  loop: [screen-top-left]
clip_chain: "one closed circular master; no independent direction clips"
rest_state: "hold last direction outside the pointer dead zone; static center frame for reduced motion/mobile"
loop: closed
anchor: bottom
scene_continuity: null
frame_policy: native
target_fps: 24
quality_target: "240x240 CSS px at DPR 2; 480x480 atlas cells; at most 64 circular samples in one 4096 texture"
reduced_motion: center
