[package]
name = "sse-probe"
version = "0.1.0"
edition = "2021"
description = "Proof that a NATIVE process reaches the local agent-office SSE — the thing a public https browser page cannot do (see docs A finding). std-only, no deps."

[[bin]]
name = "sse-probe"
path = "src/main.rs"
