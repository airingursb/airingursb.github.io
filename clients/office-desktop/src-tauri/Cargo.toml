[package]
name = "office-desktop"
version = "0.1.0"
edition = "2021"
description = "Agent Office desktop client (Tauri v2 spike) — streams the local agent-office SSE natively and renders ursb.me/nook?room=office."

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[[bin]]
name = "office-desktop"
path = "src/main.rs"
