{
  "name": "agent-office",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Visualize your local Claude Code main agent + subAgents as characters working in an office (nook Agent Office v1, single-machine).",
  "bin": { "agent-office": "server.js" },
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "install-hooks": "node install-hooks.mjs"
  }
}
