# A like received

Extends the existing article likes capsule and single watercolor panda. Keep all original typography, count, border and dimensions. No additional character or visible controls.

After the server confirms a new like, one 14px coral heart travels from the existing button to the currently visible panda. H3 supplies the full-body catch, glance and careful hug. On desktop use the floating companion when the inline dock is out of view; otherwise use the existing dock. On mobile use only the visible inline dock. A dismissed pet stays dismissed. An unlike, error or initial saved-like hydration cannot celebrate.

The capsule compresses to .96 while pressed and springs back in 320ms. Confirmed success brings a brief rose wash, a .72 → 1.32 → 1.08 → 1.15 heart pop, six small coral glints around the icon, and a 6px count lift. All presentation lives inside or immediately around the original button footprint; no additional badge or message. The color remains the original liked red, with soft coral #d89987 for traveling hearts and glints. Unlike, hidden tabs, reduced motion and teardown cancel temporary button effects.

The heart leaves the button's icon along a 520ms curved flight, synchronized to the H3 frames in one animation loop. Play the existing atlas at 1.6×, starting at frame 14 with the book or 9 without it; preserve the entire catch, hug and return. This brings the performance from 11 seconds to about 6.2–6.4 seconds, with the receiving pose within the first second. The performance stays inside the panda's existing footprint and then restores its normal renderer. One panda performance per page; each confirmed like may animate the button. Screen readers keep the existing like control state; decorations are aria-hidden and pointer-events:none.

No autoplay before confirmed interaction, no audio. Reduced motion skips both flight and performance. Hidden tabs, dismissal, navigation, resize or a target leaving the viewport cancel cleanly. Decode the new atlas only on demand; failures leave the normal pet and real like state intact. Do not delay or change API results for animation.

Reference: existing BlogPetWidget and ArticleReadingDock; beui action-swap consulted for separating state change from transient presentation. The beui motion button's source was inspected for press/release scaling, contained ripple and reduced-motion handling; implement those mechanisms with native Web Animations, without a React dependency. Use cubic-bezier(.22,1,.36,1) for release and flight accents. New art uses the existing soft watercolor panda, opaque cream fur and complete ears/feet. Inspect first, catch, hug and final frames over #ffffff and #0d1117; no white/colored matte fringe.

QA: confirmed success versus unlike/error/hydration; dock versus floating; mobile dock offscreen; stored dismissal; blocked asset; reduced motion; rapid inputs; EN/CN; preserve the prominent likes button.
